doctype html
include includes/head.pug
script(src='/assets/js/like.js')
body
    include includes/nav.pug
    #content
        #cards
            div.like.card
                if (bot.nsfw)
                    img.icon.nsfw(src=val.logo)
                else
                    img.icon(src=bot.logo)
                h2.title #{bot.username}

                p Você pode curtir a cada 12 horas 
                a.small.button()#like Vota
                p#likes(style='display: none;') #{`${bot.likes || 0} Votos`}
        br
